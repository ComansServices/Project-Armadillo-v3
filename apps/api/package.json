{
  "name": "@armadillo/api",
  "private": true,
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "echo api build pending",
    "lint": "echo api lint pending",
    "test": "echo api test pending",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:push": "prisma db push",
    "prisma:migrate:startup": "sh ./scripts/migrate-startup.sh",
    "assets:enforce-identity": "tsx src/scripts/enforce-asset-identity.ts",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bullmq": "^5.17.0",
    "fast-xml-parser": "^4.5.0",
    "fastify": "^4.28.1",
    "ioredis": "^5.4.1",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.1"
  }
}
